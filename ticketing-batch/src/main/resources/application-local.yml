spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ticketing
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: 127.0.0.1
      password: 1234
      port: 6379

  jpa:
    hibernate:
      ddl-auto: update

      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  batch:
    jdbc:
      initialize-schema: always
    job:
      enabled: false
      name: ${job.name}

job:
  seatOccupyJob:
    showId: ${job.seatOccupyJob.showId}
  waitingJob:
    timeout: 7
    showId: ${job.waitingQueue.showId}
  lock:
    key: ${job.lockKey}
  chunk:
    size: ${chunk.size}

scheduler:
  job:
    name: ${job.schedulerJob.name}

portone:
  api:
    key: PORTONE_API_KEY
    secret: PORTONE_API_SECRET

logging:
  level:
    org:
      hibernate:
        SQL: debug
        type: trace


cronexpression: '0/1 * * * * ?'